get:
  tags:
    - Book
  description: Get all books
  operationId: findAll
  responses:
    "200":
      description: Successful operation.
      content:
        application/json:
          schema:
            $ref: '../components/schemas/Book/BookList.yaml'
